# Frappe backend
VITE_FRAPPE_URL=https://app.makkobillischool.com
# OAuth Client registered in Frappe (OAuth Client doctype), redirect URI <origin>/oauth/callback
# "MBS Staff PWA" client already registered on the site:
VITE_OAUTH_CLIENT_ID=ci3eeqp3lv
